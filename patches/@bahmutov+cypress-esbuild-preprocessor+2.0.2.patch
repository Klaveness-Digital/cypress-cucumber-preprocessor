diff --git a/node_modules/@bahmutov/cypress-esbuild-preprocessor/src/index.js b/node_modules/@bahmutov/cypress-esbuild-preprocessor/src/index.js
index 85f7134..804f867 100644
--- a/node_modules/@bahmutov/cypress-esbuild-preprocessor/src/index.js
+++ b/node_modules/@bahmutov/cypress-esbuild-preprocessor/src/index.js
@@ -4,11 +4,11 @@ const debug = require('debug')('cypress-esbuild-preprocessor')
 // bundled[filename] => promise
 const bundled = {}
 
-const bundleOnce = ({ filePath, outputPath, esBuildUserOptions }) => {
+const bundleOnce = async ({ filePath, outputPath, esBuildUserOptions }) => {
   debug('bundleOnce %s', filePath)
   const started = +new Date()
 
-  esbuild.buildSync({
+  await esbuild.build({
     ...esBuildUserOptions,
     entryPoints: [filePath],
     outfile: outputPath,
@@ -22,13 +22,13 @@ const bundleOnce = ({ filePath, outputPath, esBuildUserOptions }) => {
 const createBundler = (esBuildUserOptions = {}) => {
   debug('ESBuild user options %o', esBuildUserOptions)
 
-  return function cypressESBuildFilePreprocessor(file) {
+  return async function cypressESBuildFilePreprocessor(file) {
     const { filePath, outputPath, shouldWatch } = file
 
     debug({ filePath, outputPath, shouldWatch })
 
     if (!shouldWatch) {
-      bundleOnce({ filePath, outputPath, esBuildUserOptions })
+      await bundleOnce({ filePath, outputPath, esBuildUserOptions })
       return outputPath
     }
 
